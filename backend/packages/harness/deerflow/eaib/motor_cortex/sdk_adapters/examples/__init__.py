"""Example SDK adapters for reference and as code templates.

These files are NOT loaded automatically. They serve as:
- Reference implementations the motor cortex agent can read to understand the SDK adapter pattern
- Starting templates the agent can copy-adapt for a new robot body
- Test fixtures

The motor cortex discovers and creates adapters at runtime through user conversation.
"""
